# Visual digest wire format

The visual on-screen digest wire format spec now lives in its own
crate, at
<https://github.com/shakenfist/visual-digest-rust/blob/main/docs/visual-digest-format.md>.
See also the `shakenfist-visual-digest` crate's source for the
authoritative encoder and decoder.
